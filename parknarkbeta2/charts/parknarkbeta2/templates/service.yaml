apiVersion: v1
kind: Service
metadata:
  name: {{ template "parknarkbeta2.fullname" . }}
  labels:
    app: {{ template "parknarkbeta2.name" . }}
    chart: {{ template "parknarkbeta2.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "parknarkbeta2.name" . }}
    release: {{ .Release.Name }}
